---
"evolit": patch
---

Document and certify direct `@litsx/tailwind` integration through the native LitSX pipeline, including Shadow DOM SSR, hydration, invalidation, deterministic production assets, and isolated concurrent builds.
