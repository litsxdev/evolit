---
"evolit": minor
---

Add `getRouteState()` as a request-scoped, read-only server API for accessing
the active URL, params, search params, and navigation context without exposing
browser history methods.
