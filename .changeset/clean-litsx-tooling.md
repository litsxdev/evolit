---
"evolit": patch
---

Remove the deprecated `@litsx/typescript` integration, use standard TypeScript
for project typechecking, and update LitSX compiler, core, and SSR dependencies
to their current `next` releases.
