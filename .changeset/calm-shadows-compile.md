---
"evolit": minor
---

Add the native LitSX integration pipeline configured through `evolit.config.js`. Applications can
now compose compiler options and build-tool-neutral integrations such as `@litsx/unocss` across
development, SSR, hydration, production builds and standalone runtimes, with isolated lifecycle
state, watched dependencies, virtual modules and content-hashed document assets.
